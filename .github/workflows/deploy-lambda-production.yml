name: Deploy Lambda Scraper (Production)

on:
  push:
    branches:
      - production
    paths:
      - 'lambda-scraper/**'
      - '.github/workflows/deploy-lambda-production.yml'
  workflow_dispatch:

jobs:
  deploy-production:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: lambda-scraper/package.json

      - name: Install Dependencies
        working-directory: lambda-scraper
        run: npm ci --production

      - name: Create Deployment Package
        working-directory: lambda-scraper
        run: |
          zip -q -r function.zip index.js package.json node_modules/
          echo "Package size: $(du -h function.zip | cut -f1)"

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID_PROD }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY_PROD }}
          aws-region: ${{ secrets.AWS_REGION_PROD }}

      - name: Update Lambda Function Code
        working-directory: lambda-scraper
        run: |
          aws lambda update-function-code \
            --function-name crypto-news-scraper-prod \
            --zip-file fileb://function.zip \
            --region ${{ secrets.AWS_REGION_PROD }}

      - name: Update Lambda Configuration
        run: |
          aws lambda update-function-configuration \
            --function-name crypto-news-scraper-prod \
            --environment Variables="{
              CLOUDFLARE_ACCOUNT_ID=${{ secrets.CLOUDFLARE_ACCOUNT_ID_PROD }},
              CLOUDFLARE_D1_DATABASE_ID=${{ secrets.CLOUDFLARE_D1_DATABASE_ID_PROD }},
              CLOUDFLARE_API_TOKEN=${{ secrets.CLOUDFLARE_API_TOKEN_PROD }}
            }" \
            --region ${{ secrets.AWS_REGION_PROD }}

      - name: Wait for Update to Complete
        run: |
          aws lambda wait function-updated \
            --function-name crypto-news-scraper-prod \
            --region ${{ secrets.AWS_REGION_PROD }}

      - name: Deployment Summary
        run: |
          echo "✅ Lambda function deployed successfully"
          echo "Function: crypto-news-scraper-prod"
          echo "Region: ${{ secrets.AWS_REGION_PROD }}"
          echo "Environment: Production"
