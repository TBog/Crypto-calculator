name: Deploy Cloudflare Workers (Production)

on:
  push:
    branches:
      - production
    paths:
      - 'worker/**'
      - '.github/workflows/deploy-workers-production.yml'
  workflow_dispatch:

jobs:
  deploy-production:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # List of worker directories to deploy
        worker: [worker-api, worker-news-updater, worker-news-processor]
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Deploy Worker to Production - ${{ matrix.worker }}
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          # Deploy from the worker directory (parent of worker-*)
          workingDirectory: worker
          # Specify the config file for this specific worker
          # Deploy to production environment using --env production
          command: deploy --config ${{ matrix.worker }}/wrangler.toml --env production
